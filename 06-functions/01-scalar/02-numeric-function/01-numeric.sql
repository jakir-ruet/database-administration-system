CREATE TABLE EMPLOYEES (
    ID NUMBER PRIMARY KEY,
    NAME VARCHAR2(100),
    DEPARTMENT VARCHAR2(50),
    SALARY NUMBER(15,7)
);

INSERT INTO EMPLOYEES VALUES (1, 'Alice', 'HR', 60000.7685000);
INSERT INTO EMPLOYEES VALUES (2, 'Bob', 'IT', 50000.1234000);
INSERT INTO EMPLOYEES VALUES (3, 'Charlie', 'Finance', 55000.5678000);
INSERT INTO EMPLOYEES VALUES (4, 'David', 'Marketing', 45000.4321000);

COMMIT;

SELECT *
FROM EMPLOYEES;

SELECT NAME,
		 SALARY,
		 ROUND(SALARY, 4) AS ROUNDED_SALARY
FROM EMPLOYEES;


SELECT NAME,
		 SALARY,
		 TRUNC(SALARY, 4) AS TRUNCATED_SALARY
FROM EMPLOYEES;
